<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="nz.co.greenjersey.inventorytracker">

    //request permission to use the internet
    <uses-permission android:name="android.permission.INTERNET"/>


    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MainMenu" />
        <activity android:name=".BatchAssign" />
        <activity android:name=".SingleAssign" />
        <activity android:name=".BatchCheckIn" />
        <activity
            android:name=".SingleCheckIn"
            android:label="@string/title_activity_single_check_in"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".BatchCheckOut" />
        <activity android:name=".SingleCheckOut" />
        <activity android:name=".AddMaintenanceNote"></activity>
    </application>

</manifest>